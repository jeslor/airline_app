node_modules/
node_modules
server.env
.vercel

/generated/prisma

# Puppeteer's downloaded Chromium (see .puppeteerrc.cjs) - redownloaded on
# every npm install, should not be committed
/.cache/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
